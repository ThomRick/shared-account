import { SharedAccountAggregate } from './shared-account.aggregate';

describe('Shared Account Aggrgate', () => {
  it('can be created', () => {
    const aggregate = new SharedAccountAggregate();
  });
});
